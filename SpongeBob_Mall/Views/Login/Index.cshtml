<div class="cen">
    <div class="table">
        <div class="logo2">
            <img src="../img/logocn.png">
        </div>
        <div class="form1">
            @using (Html.BeginForm("Login", "Login", FormMethod.Post,new { @onsubmit = "return validate_form(this)" }))
            {
                @Html.TextBox("username", "", new { @class = "form11", @placeholder = "用户名" });
                @Html.Password("password", "", new { @class = "form11", @placeholder = "密码" });
                <input type="submit" value="登录" class="login-in-btn" />
            }
            
        </div>
        @Html.ActionLink("注册","Register","Login",new {@class="login-in-btn2" })
    </div>
</div>